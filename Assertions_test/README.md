# Assertions Testing

A small `pytest` project demonstrating common assertions for collections and floating-point values.

## Files

- `test_collections.py` tests list, dictionary, and set assertions.
- `test_floats.py` demonstrates reliable float comparisons with `pytest.approx`.

## Run the tests

Install `pytest` if needed, then run:

```bash
pytest
```
